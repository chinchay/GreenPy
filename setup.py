from setuptools import setup

setup(
    name='GreenPy',
    version='0.1',
    description='Decimation of the Green function in graphene nanoribbons',
    packages=['GreenPy'],
    zip_safe=False
    )